class LogIn {
    constructor(id_login, data_login, data_expiracao) {
        this._id_login = id_login
        this._data_login = data_login
        this._data_expiracao = data_expiracao
    }
}

module.exports = LogIn